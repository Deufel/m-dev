[project]
name = "m-dev"
version = "0.0.1"
description = "Build and publish Python packages directly from marimo notebooks"
authors = [
    {name = "Deufel", email = "MDeufel13@gmail.com"},
]
license = {text = "MIT"}
requires-python = ">=3.13"
dependencies = ['pytest==8.4.2']
